{% comment %} Amazo Theme - Page Template {% endcomment %}
<div class="page-width" style="padding: 20px 0;">
  {%- render 'breadcrumb' -%}
  <div class="page-content">
    <h1 class="page-content__title">{{ page.title }}</h1>
    <div class="rich-text-section">{{ page.content }}</div>
  </div>
</div>
