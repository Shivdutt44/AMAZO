{% comment %} AMAZO Homepage — Amazon.com Style v3 {% endcomment %}

<!-- ── ANNOUNCEMENT BAR ── -->
{% section 'announcement-bar' %}

<!-- ── HERO CAROUSEL ── -->
{% section 'hero-banner' %}

<!-- ── 4-COLUMN HOMEPAGE CARDS: 3 Featured Product Cards + Deal of the Day ── -->
<div class="amz-homepage-cards">
  <div class="amz-homepage-cards__grid">
    {% section 'homepage-top-cards' %}
    <div class="amz-homepage-cards__card amz-homepage-cards__card--deals">
      {% section 'deals-of-the-day' %}
    </div>
  </div>
</div>

<!-- ── BEST SELLERS PRODUCT SLIDER ── -->
{% section 'amazon-product-slider' %}

<!-- ── TRUST BADGES ── -->
{% section 'trust-badges' %}

<!-- ── CATEGORY GRID ── -->
{% section 'category-grid' %}

<!-- ── PROMO BANNERS ── -->
<div class="page-width" style="padding: 8px 10px;">
  {% section 'promo-banners' %}
</div>

<!-- ── NEWSLETTER ── -->
{% section 'newsletter' %}
